plugins {
    id 'com.android.application'
}

android {
    namespace 'com.endroid.screenoff'
    compileSdk 34

    defaultConfig {
        applicationId "com.endroid.screenoff"
        minSdk 28
        targetSdk 34
        versionCode 4
        versionName "1.2.0"
        resourceConfigurations += ['en']
    }

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_1_8
        targetCompatibility JavaVersion.VERSION_1_8
    }

    signingConfigs {
        debugLocal {
            storeFile file("${System.properties['user.home']}/.android/debug.keystore")
            storePassword 'android'
            keyAlias 'androiddebugkey'
            keyPassword 'android'
        }
    }

    buildTypes {
        debug {
            minifyEnabled false
        }
        release {
            minifyEnabled true
            shrinkResources true
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
            signingConfig signingConfigs.debugLocal
        }
    }

    packaging {
        resources {
            excludes += ['META-INF/**', 'kotlin/**', '**.kotlin_module']
        }
    }
}

dependencies {
    // Zero third-party libraries — framework only for minimal APK size.
}
